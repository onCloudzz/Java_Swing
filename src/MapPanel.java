import javax.swing.*;
import java.awt.*;

public class MapPanel extends JPanel {
    public MapPanel() {
        setBackground(Color.cyan);
        setPreferredSize(new Dimension(300, 200));
        setVisible(true);
    }
}
